"""
📚 LangGraph 패턴별 학습 가이드

================================================================================
🎯 이 폴더는 무엇인가요?
================================================================================

원본 파일 `langgraph_comprehensive_practice.py` (1800줄)을
**8개의 작은 파일**로 분리한 교육용 폴더입니다.

[장점]
- 한 번에 하나의 개념만 집중해서 배울 수 있습니다.
- 파일이 짧아서 읽기 쉽습니다.
- 단독 실행이 가능해서 바로 테스트할 수 있습니다.

================================================================================
📂 이 폴더(langgraph_comprehensive_practice)의 구성
================================================================================

📁 langgraph_comprehensive_practice/
├── shared.py                      # [Step 0] 공용 모듈 (AI 모델, 도구)
├── pattern1_simple_routing.py     # [Step 1] 기본 2-way 라우팅
├── pattern2_advanced_routing.py   # [Step 2] 3-way 라우팅
├── pattern3_react_agent.py        # [Step 3] ReAct 에이전트
├── pattern4_chat_memory.py        # [Step 4] 대화 기억
├── pattern0_master_router.py      # [Step 5] 마스터 라우터 (반장)
├── main_all_patterns.py           # [Step 6] 통합 실행 파일
└── README.py                      # 이 파일 (학습 가이드)

================================================================================
🏗️ 개발(학습) 순서 가이드 (Development Order)
================================================================================

이 순서는 "실제로 코드를 작성할 때" 의존성 순서입니다.
이대로 따라 읽으시면 "재료 준비 → 부품 제작 → 조립" 과정을 이해할 수 있습니다.

--------------------------------------------------------------------------------
[Step 0] shared.py (재료 손질 🥕)
--------------------------------------------------------------------------------
- **가장 먼저 보세요!**
- 모든 패턴에서 사용할 AI 모델과 도구(계산기)를 여기서 먼저 정의합니다.
- "공통 모듈을 먼저 만든다"는 원칙을 배웁니다.

[핵심 학습 포인트]
- ChatOpenAI 모델 초기화
- 도구(calculator_tool, knowledge_search_tool) 정의
- 왜 전역 변수로 만드는지

--------------------------------------------------------------------------------
[Step 1] pattern1_simple_routing.py (기초 골격 🦴)
--------------------------------------------------------------------------------
- 가장 쉬운 2가지 분기(Technical/Casual)를 만듭니다.
- State와 Graph의 기본 모양을 잡습니다.

[핵심 학습 포인트]
- State(상태)란 무엇인가?
- Node(노드)는 어떻게 정의하는가?
- Conditional Edge(조건부 분기)는 어떻게 연결하는가?
- 그래프를 조립(build)하고 컴파일(compile)하는 방법

--------------------------------------------------------------------------------
[Step 2] pattern2_advanced_routing.py (확장하기 ➕)
--------------------------------------------------------------------------------
- 패턴 1 복사 후, 분기를 3개(Creative 추가)로 늘려봅니다.
- "기존 코드를 어떻게 확장하는가"를 배웁니다.

[핵심 학습 포인트]
- 패턴 1과의 차이점 파악
- 딕셔너리 .get() 메서드를 활용한 깔끔한 라우팅
- 페르소나(Persona) 설계

--------------------------------------------------------------------------------
[Step 3] pattern3_react_agent.py (도구 연결 🔧)
--------------------------------------------------------------------------------
- 단순 대화가 아니라 `shared.py`의 '계산기'를 실제로 써봅니다.
- 그래프가 뱅글뱅글 도는 '루프(Loop)'를 구현합니다.

[핵심 학습 포인트]
- 순환 그래프(Cyclic Graph)란?
- 도구(Tools) 사용법
- 무한 루프 방지 패턴 (iterations 카운터)
- Think-Act-Observe 패턴

--------------------------------------------------------------------------------
[Step 4] pattern4_chat_memory.py (기억력 주입 🧠)
--------------------------------------------------------------------------------
- 가장 어렵습니다. DB 역할(세션 저장소)을 추가합니다.
- 대화 내용을 기억하도록 만듭니다.

[핵심 학습 포인트]
- Session(세션) 관리
- 대화 히스토리 저장
- 종료 키워드 처리
- 워커(Worker) 문제와 해결책

--------------------------------------------------------------------------------
[Step 5] pattern0_master_router.py (최종 조립 🤖)
--------------------------------------------------------------------------------
- 위 1~4번 부품들이 다 완성되었으니, 이걸 하나로 묶습니다.
- "사용자 질문을 분석해 자동으로 부품을 골라주는" 반장을 만듭니다.

[핵심 학습 포인트]
- Supervisor(감독자) 패턴
- Intent Classification (의도 분류)
- 여러 그래프 조합

--------------------------------------------------------------------------------
[Step 6] main_all_patterns.py (배포 준비 🚀)
--------------------------------------------------------------------------------
- 모든 코드를 import해서 하나의 서버로 띄웁니다.
- 실제 원본 파일(`langgraph_comprehensive_practice.py`)이 이 모습입니다.

[핵심 학습 포인트]
- 모듈 조합
- 여러 엔드포인트 노출
- 실무 배포 구조

================================================================================
🚀 각 파일 실행 방법
================================================================================

1. 터미널에서 이 폴더로 이동:
   cd "C:\\AI Projects\\codeit\\코드잇 AI\\실습\\fastapi-실습-2\\lab4\\langgraph_comprehensive_practice"

2. 원하는 패턴 실행:
   python pattern1_simple_routing.py
   
3. 브라우저에서 테스트:
   http://localhost:8000/docs

4. 다른 패턴 테스트 시:
   - 기존 서버 종료 (Ctrl+C)
   - 다른 패턴 파일 실행

================================================================================
💡 학습 팁
================================================================================

1. **한 번에 한 파일만 집중하세요.**
   - 여러 파일을 동시에 보면 헷갈립니다.
   - Step 0부터 차근차근!

2. **주석을 꼼꼼히 읽으면서 코드를 따라 치세요.**
   - 복붙보다 직접 치면 이해가 빨라집니다.
   - Q&A 섹션을 놓치지 마세요!

3. **직접 질문을 바꿔보면서 테스트하세요.**
   - "파이썬이 뭐야?" → "자바스크립트가 뭐야?"
   - 결과가 어떻게 달라지는지 확인하세요.

4. **이해가 안 되면 원본 파일과 비교해보세요.**
   - 상위 폴더의 `langgraph_comprehensive_practice.py`

================================================================================
⚠️ 주의사항
================================================================================

1. **shared.py가 같은 폴더에 있어야 합니다!**
   - 다른 패턴 파일들이 shared.py를 import합니다.
   - 삭제하면 모든 파일이 에러 납니다.

2. **.env 파일에 OPENAI_API_KEY가 설정되어 있어야 합니다.**
   - 프로젝트 루트( 또는 이 폴더)에 .env 파일이 있어야 합니다.
   - 예: OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxx

3. **한 번에 하나의 서버만 실행할 수 있습니다.**
   - 포트 8000을 공유하므로, 여러 서버를 동시에 띄우면 충돌합니다.
   - 기존 서버 종료 후 다른 서버 실행하세요.

4. **패턴 4는 세션을 먼저 시작해야 합니다.**
   - /start로 세션 생성 → /chat으로 대화
   - 순서를 지키지 않으면 404 에러!

================================================================================
📞 도움이 필요하면?
================================================================================

- 원본 파일의 상단 가이드를 읽어보세요.
- 각 파일의 상단 독스트링에 상세 설명이 있습니다.
- Q&A 주석을 찾아보세요 (예: "[Q&A] 왜 async인가요?")

================================================================================
"""

if __name__ == "__main__":
    # 이 파일을 직접 실행하면 가이드 내용을 출력합니다.
    print(__doc__)
    
    print("=" * 60)
    print("💡 시작하려면 아래 명령어를 실행하세요:")
    print("   python shared.py")
    print("=" * 60)
